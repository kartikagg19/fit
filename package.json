{
  "name": "fitgym-deployment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && cp -r frontend/build ./build"
  }
}
